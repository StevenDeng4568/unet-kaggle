# Kaggle 上传说明

"
    "1. 在 Kaggle 创建一个 Dataset（例如 `my-unet-code`）。
"
    "2. 上传 `export_unet.zip`（或解压后上传整个目录）。
"
    "3. 在 Kaggle Notebook 里 Add Data，绑定该 Dataset。
"
    "4. 代码路径通常在 `/kaggle/input/<dataset-name>/`。
"
    "5. 训练/推理产物请保存到 `/kaggle/working/`，方便下载。
"
    